spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: 
          default-label: main
          username: 
          password: x-oauth-basic
server:
  port: 8888

